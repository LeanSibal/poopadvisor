import React, { Component } from 'react';
import {
    View,
    Text
} from 'react-native';
import LocationReview from '../components/LocationReview';

class LocationRate extends Component {
    render() {
        return (
            <LocationReview />
       );
    }
}

export default LocationRate;
